// add natural number
const sumOfSquareNum = (num) => {
  const S = (num * (num + 1) * (2 * num + 1) / 6)

  alert(`The result of sum of 50 is ${S} where input is ${num}`)
}
console.log(sumOfSquareNum(5));

